# Supabase Edge Function secrets. Never use a token committed to Git.
CLARITY_API_TOKEN=
CLARITY_PROJECT_ID=xogpsbqaar
CLARITY_API_BASE_URL=https://www.clarity.ms/export-data/api/v1
CLARITY_CRON_SECRET=

# Mock data is fail-closed. Both flags are required and production is rejected.
CLARITY_MOCK_MODE=false
CLARITY_ENVIRONMENT=production
